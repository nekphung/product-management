header.admin-header
  .admin-header__brand
    button.admin-menu-toggle(type="button" data-sidebar-toggle aria-label="Mở menu quản trị") ☰
    a(href=`${prefixAdmin}/dashboard`) Admin Panel
  .admin-header__actions
    a.admin-header__account(href=`${prefixAdmin}/my-account`) #{user.fullName}
    a.btn.btn-outline-light.btn-sm(href=`${prefixAdmin}/auth/logout`) Đăng xuất
