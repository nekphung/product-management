mixin product-item(item)
    .col-4.mb-3
        .product-item
            .inner-image
                img(
                    src=item.thumbnail, 
                    alt=item.title
                )
                if(item.featured == "1") 
                    .inner-featured Nổi bật 

            .inner-content
                h3.inner-title 
                    a(href=`/products/detail/${item.slug}`) #{item.title}
                .inner-price-new #{parseInt(item.priceNew).toLocaleString('vi-VN')}đ
                .inner-price-old #{parseInt(item.price).toLocaleString('vi-VN')}đ
                .inner-discount -#{item.discountPercentage}%